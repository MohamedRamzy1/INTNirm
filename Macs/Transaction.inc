Transaction MACRO
	
TRANSACTION:
    MOV BX,OFFSET BULLETS1
    MOV [BX],0
    MOV BX,OFFSET BULLETS2
    MOV [BX],0
    MOV AL,0
    MOV NumberOfBullets1,AL
    MOV NumberOfBullets2,AL
    MOV STAGE,2
	CALL AFFECTR1
	CALL AFFECTR2
	MOV BYTE PTR SIDE1[1],X2POS
	MOV BYTE PTR SIDE2,X1POS

ENDM Transaction